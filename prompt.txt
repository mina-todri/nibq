Flutter project has a runtime crash:

ERROR:
Bad state: Cloudinary credentials not configured.
Missing --dart-define values:
- CLOUDINARY_CLOUD_NAME
- CLOUDINARY_UPLOAD_PRESET

PROBLEMS:
- App crashes in product/admin/home screens due to missing Cloudinary config
- No safe fallback handling for missing environment variables

TASKS:

1. Fix Cloudinary initialization:
   - Use String.fromEnvironment for both CLOUD_NAME and UPLOAD_PRESET
   - Ensure safe validation before usage

2. Prevent app crash:
   - Replace any direct throw causing full app crash
   - Add safe error handling in service layer (product_service.dart or Cloudinary service)

3. UI safety:
   - Ensure screens (admin_dashboard, home/products) show ErrorStateWidget instead of red screen when Cloudinary fails

4. Behavior requirement:
   - If config missing → fail gracefully (no crash)
   - Show user-friendly error message only

CONSTRAINTS:
- Do NOT add new packages
- Do NOT refactor architecture
- Do NOT change business logic
- Only fix crash + add safe handling